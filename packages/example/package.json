{
  "name": "@wasiglk/example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run serve.ts",
    "build": "bun build src/main.ts --outdir=dist --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wasiglk/client": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
